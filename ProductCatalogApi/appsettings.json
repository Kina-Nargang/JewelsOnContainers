{ // this file will be only needed when run on IISExpress
  // when run on docker we need to set up all info below in the docker compose file 
  "ConnectionString": "Data Source=(localdb)\\MSSQLLocalDB;Initial Catalog=CatalogDb;Integrated Security=True;Connect Timeout=30;",
  "ExternalCatalogBaseUrl": "http://localhost:51555",
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "AllowedHosts": "*"
}
